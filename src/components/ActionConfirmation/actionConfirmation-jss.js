const styles = (theme) => ({
  dialogActionContainer: {
    marginTop: '15px',
    textAlign: 'right',
  },
  dialogActionBtn: {
    '&.MuiButton-root': {
      textTransform: 'none',
      fontSize: '17px',
      borderRadius: '10px',
      fontWeight: 500,
    },
  },
});

export default styles;
