const styles = (theme) => ({
  boardItemContainer: {
    '&.MuiPaper-root': {
      padding: '15px',
      marginBottom: '15px',
      borderRadius: '10px',
    },
  },
  divider: {
    '&.MuiDivider-root': {
      marginBottom: '15px',
    },
  },
  activityContent: {
    borderRadius: '10px',
    border: '1px solid rgba(0, 0, 0, 0.87)',
    padding: '10px',
  },
});

export default styles;
