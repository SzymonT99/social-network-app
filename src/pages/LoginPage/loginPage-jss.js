const styles = (theme) => ({
  wrapper: {
    padding: '80px 60px',
    display: 'flex',
    flexDirection: 'column',
  },
  link: {
    padding: '20px',
    textDecoration: 'none',
    textAlign: 'center',
  },
});

export default styles;
